description: Artist can only select his/her tracks. Without sending header (Error)
url: /v1/graphql
status: 200
headers:
  X-Hasura-Role: Artist
response:
  errors:
  - extensions:
      code: unexpected
      path: "$"
    message: 'missing required session variable: "x-hasura-artist-id"'
query:
  query: |
    query {
      Track {
        id
        name
      }
    }
